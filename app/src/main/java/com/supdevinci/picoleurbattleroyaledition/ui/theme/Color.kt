package com.supdevinci.picoleurbattleroyaledition.ui.theme

import androidx.compose.ui.graphics.Color

// — Background & Surface (dark navy, same as splash) —
val Navy900 = Color(0xFF1A1A2E)   // deepest background
val Navy800 = Color(0xFF16213E)   // surface
val Navy700 = Color(0xFF0F3460)   // surface variant / elevated

// — Primary — vivid pinkish-red (energy / party vibe) —
val CrimsonRed         = Color(0xFFE94560)
val CrimsonOnPrimary   = Color(0xFFFFFFFF)
val CrimsonContainer   = Color(0xFF5C0A1E)
val CrimsonOnContainer = Color(0xFFFFB3BC)

// — Secondary — gold (leaderboard / prestige) —
val Gold               = Color(0xFFFFD060)
val GoldOnSecondary    = Color(0xFF1A1A2E)
val GoldContainer      = Color(0xFF4A3800)
val GoldOnContainer    = Color(0xFFFFE87A)

// — Tertiary — teal accent —
val Teal               = Color(0xFF00BCD4)
val TealOnTertiary     = Color(0xFF1A1A2E)
val TealContainer      = Color(0xFF003640)
val TealOnContainer    = Color(0xFFB2EBF2)

// — Neutrals —
val OnDark             = Color(0xFFE8EAF6)
val OnDarkMuted        = Color(0xFFB0BEC5)

// — Error —
val ErrorRed           = Color(0xFFCF6679)
val ErrorOnError       = Color(0xFF1A1A2E)
val ErrorContainer     = Color(0xFF93000A)
